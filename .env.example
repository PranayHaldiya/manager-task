# Database URL (Use Vercel Postgres or your own PostgreSQL database)
POSTGRES_URL="postgres://username:password@localhost:5432/database_name"

# NextAuth Configuration
# For production, set this to your Vercel deployment URL (https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
# Generate a random secret: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-secret-key"

# Vercel specific environment variables are automatically added during deployment 